pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Compilation du projet'
            }
        }

        stage('Tests') {
            steps {
                echo 'Exécution des tests'
            }
        }

        stage('Lint') {
            steps {
                echo 'Analyse du code'
            }
        }

    }
}
